# TB-85: Docs flip: IMPLEMENTATION.md M7 + FEATURES.md F7.1/F7.2/F7.3 markers + ARCHITECTURE.md if needed

**Type:** tech-debt
**Priority:** P2
**Size:** S
**Module:** gui
**Tags:** milestone-m7,docs
**Branch:** —
**Parent:** TB-7

## Goal

After all M7 children land, flip the marker squares in the docs and add a "Completed work log" entry — same pattern as the M5 / M6 doc-flip tasks. Add an ARCHITECTURE.md note ONLY if the menu/tray work introduced a load-bearing invariant (e.g. a state contract between the daemon and the tray icon's run counter).

## Context

Per CLAUDE.md "When to update docs": treat `docs/` as code; the flip is the seal on a milestone. Mirrors TB-74 (M6 doc flip) in shape.

`IMPLEMENTATION.md` § M7 currently reads `## M7 — Polish · ⊘ (optional)`. After completion, change the marker to `☑` and replace the body with a real task list reflecting what shipped (what was done vs. ⊘'d if a child like the tray got deferred).

`FEATURES.md` F7.1 / F7.2 / F7.3 markers flip from `☐` to `☑` for whatever shipped. Anything ⊘'d gets a one-line note (`⊘ deferred to post-M7 — see TB-NNN`).

**New feature to introduce**: TB-82 (native application menu) has no anchor in `FEATURES.md` today. This docs-flip task is responsible for adding **F7.4 — Native application menu** alongside flipping it to ☑ (or ⊘ if TB-82 was deferred). Use TB-82's AC list as the source of truth. Without F7.4 the docs imply M7 is done at three features when it actually shipped four.

ARCHITECTURE.md update only if needed — for example, if TB-83's tray run-counter becomes a documented contract with the AgentService event vocabulary, add a "Tray state" subsection. Otherwise leave ARCHITECTURE.md alone.

## Acceptance Criteria

- [x] `docs/IMPLEMENTATION.md` § M7 marker → `☑` (or `⬚` if any child deferred); body lists shipped items + any deferrals
- [x] `docs/FEATURES.md` F7.1 / F7.2 / F7.3 markers updated to match shipped vs. deferred state
- [x] `docs/FEATURES.md` § M7 gains **F7.4 — Native application menu** (with AC mirroring TB-82) and is marked ☑ / ⊘ to match TB-82's outcome
- [x] "Completed work log" entry in `IMPLEMENTATION.md` summarising M7 (1 paragraph, same density as the M5 entry)
- [x] If any child is deferred: a follow-up backlog task exists with the deferral context, and the ⊘ marker links to it
- [x] ARCHITECTURE.md updated only if menu/tray work introduced a documented invariant — otherwise no change

## Related Tasks

- **TB-7** — parent epic this seals
- **TB-74** — M6 docs-flip (precedent for shape + density)
- All other M7 children (TB-76..TB-84)

## Log

- 2026-05-14: Created
- 2026-05-14: Done
- 2026-05-14: Done — flipped M7 docs to shipped state, added F7.4 native-menu feature anchor, and documented shell/tray responsibilities in architecture.
