# TB-10: Migrate task .md writes to writeFileAtomic

**Type:** tech-debt
**Priority:** P1
**Size:** S
**Module:** cli
**Tags:** milestone-m1,atomic-writes
**Branch:** —
**Parent:** TB-1

## Goal

Replace every direct os.WriteFile for a task .md file with writeFileAtomic in cli/create.go, cli/edit.go, cli/move.go, cli/board.go (archiveTask), and cli/scan.go. Direct os.WriteFile on .md files outside cli/atomicfs.go becomes forbidden.

## Context

After TB-9 lands, every task `.md` writer must call `writeFileAtomic`. The plan enumerates the sites. After this task, direct `os.WriteFile` on a `.md` file outside `cli/atomicfs.go` is forbidden — codified by a comment at each migrated site and verifiable via `grep`.

## Acceptance Criteria

- [ ] `cli/create.go` — new task file write via `writeFileAtomic` (~line 128)
- [ ] `cli/create.go` — `addTagToTaskFile` and `addChildToSubtasks` writes via `writeFileAtomic` (~lines 229, 252, 257, 267, 273)
- [ ] `cli/edit.go` — task edit write via `writeFileAtomic` (~line 116)
- [ ] `cli/move.go` — move-destination write via `writeFileAtomic` (~line 135)
- [ ] `cli/board.go` — `archiveTask` destination write via `writeFileAtomic` (~line 335)
- [ ] `cli/scan.go` — scan-created task via `writeFileAtomic` (~line 122)
- [ ] `grep -nE 'os\\.WriteFile\\([^)]*\\.md' cli/` returns no hits outside `cli/atomicfs.go`
- [ ] Existing tests still pass

## Related Tasks

- **TB-1** — Parent epic
- **TB-9** — Prerequisite (helper)

## Log

- 2026-05-13: Created
- 2026-05-13: Started — moved to in-progress
- 2026-05-13: Done
