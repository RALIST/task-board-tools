# TB-20: fsnotify watcher with debounce and Wails events

**Type:** feature
**Priority:** P1
**Size:** M
**Module:** gui
**Tags:** milestone-m2,watcher
**Branch:** —
**Parent:** TB-2

## Goal

Implement gui/internal/watcher/watcher.go using fsnotify. Watches board/{backlog,in-progress,done,archive}/. Ignores BOARD.md, .next-id, .board.lock, .agent-state/, .agent-logs/. Debounce 200ms. Emits Wails events board:reloaded on Create/Remove/Rename and task:updated:<id> on Write.

## Context

Live update path for the GUI. Every CLI mutation eventually writes a task `.md` and then a fresh `BOARD.md`. The watcher translates filesystem activity into two coarse-grained Wails events: a column-level invalidation (`board:reloaded`) and a single-task invalidation (`task:updated:<id>`). Debounce coalesces the fan-out from a single logical mutation (move = remove + create + regenerate). Plan risk #2 explicitly calls out the ignore list — emitting on `BOARD.md` would create a feedback loop because `tb regenerate` rewrites it after every mutation. The watcher depends on atomic writes (TB-9/TB-10) so it never sees a partially written file.

## Acceptance Criteria

- [x] `gui/internal/watcher/watcher.go` exports `Watcher` with `Start(ctx, boardDir)` and `Switch(boardDir)` for `OpenBoard`
- [x] Subscribes to `board/{backlog,in-progress,done,archive}/` (missing dirs silently skipped — fresh boards may have only some)
- [x] Ignores `BOARD.md`, `.next-id`, `.board.lock`, `.gitkeep`, `.DS_Store`, anything under `.agent-state/`, anything under `.agent-logs/`
- [x] 200ms debounce window — verified by `TestDebounce_CoalescesBurst` (remove + create + regenerate → single emit)
- [x] `Create`, `Remove`, `Rename` events emit `board:reloaded` via the debouncer
- [x] `Write` events on a task `.md` emit `task:updated:<id>` (id stripped from filename; ignored basenames suppressed)
- [x] Graceful shutdown on `ctx.Done()` — pump goroutine cancels via its own context, fsnotify watcher closed in its `defer`, debouncer stopped
- [x] Integration test drives the real `tb` binary (`tb init` → `tb create` → `tb mv`) and asserts exactly one `board:reloaded` within 1s (`TestIntegration_TBMvFiresOneBoardReloaded`)

## Related Tasks

- **TB-2** — Parent epic
- **TB-17** — Prerequisite (scaffold)
- **TB-9, TB-10** — Prerequisite (atomic writes — without them the watcher reads truncated files)
- **TB-13** — Sister fix (ensures regenerate fires after `create`/`edit`)
- **TB-21** — Consumer (`OpenBoard` calls `Switch`)
- **TB-22** — Frontend consumer

## Log

- 2026-05-13: Created
- 2026-05-13: Started — moved to in-progress
- 2026-05-13: Implemented `Watcher` in `gui/internal/watcher/watcher.go`. Pump-goroutine design: each `attach` spawns a pump that copies fsnotify events onto a single stable internal channel; `Start` reads only from that channel and is unaffected by `Switch`. Initial design (read fsw.Events directly in Start) broke `Switch` because closing the old fsw closed its channel and ended Start's loop — caught by `TestSwitch_RetargetsToNewBoard`.
- 2026-05-13: 8 unit tests cover Create/Write/debounce/ignore/Switch/empty-dir/taskIDFromPath. Integration test drives the real `tb` binary end-to-end.
- 2026-05-13: Done — TB-21 / TB-22 unblocked.
- 2026-05-13: Done
