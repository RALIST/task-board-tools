# TB-17: Scaffold gui/ Wails3 + SvelteKit project with single-instance lock

**Type:** feature
**Priority:** P1
**Size:** S
**Module:** gui
**Tags:** milestone-m2,scaffold
**Branch:** —
**Parent:** TB-2

## Goal

Run wails3 init -t sveltekit-ts in gui/, add ./gui to the root go.work, enable the Wails3 single-instance feature, and verify wails3 dev launches a blank window. Foundation for all other M2 frontend/backend work.

## Context

Bootstraps the gui/ module. After this lands, all subsequent M2 tasks have a real project to land code in. Single-instance lock is included here because it is a Wails3 platform feature wired during init — far cheaper to set up at scaffold time than to retrofit later, and it protects the daemon uniqueness invariant in M5.

## Acceptance Criteria

- [x] `wails3 init -t sveltekit-ts` produces a clean `gui/` tree
- [x] Root `go.work` updated with `use ./gui`
- [x] `go build ./gui` from the repo root succeeds
- [ ] `cd gui && wails3 dev` launches a blank window and exits cleanly on Ctrl+C — deferred to end-of-epic `/ui-test`
- [x] Wails3 single-instance feature enabled — `SingleInstanceOptions` wired in `main.go` with `OnSecondInstanceLaunch` that restores+focuses; runtime acceptance deferred to `/ui-test`
- [x] `gui/go.mod` pins the Wails3 tag confirmed in TB-16 (`v3.0.0-alpha.91`)
- [x] Repo `.gitignore` updated to exclude scaffold artefacts (`/gui/frontend/node_modules`, `/gui/frontend/dist`, `/gui/frontend/.svelte-kit`, `/gui/bin`, `/gui/.task`, `/gui/tb-gui`); `gui/.gitignore` also lists `frontend/bindings/`
- [x] App binary name and bundle identifier set to `tb-gui` in the Wails config (`build/config.yml`: productName `tb-gui`, productIdentifier `com.taskboard.tbgui`)

## Related Tasks

- **TB-2** — Parent epic
- **TB-16** — Prerequisite (toolchain validated)
- **TB-18, TB-19, TB-20, TB-21, TB-22** — All blocked until the scaffold exists

## Log

- 2026-05-13: Created
- 2026-05-13: Started — moved to in-progress
- 2026-05-13: `wails3 init -t sveltekit-ts -n tb-gui -mod tools/tb-gui` ran cleanly; scaffold flattened from `gui/tb-gui/*` to `gui/*` (init created a nested dir). Demo `GreetService` + 1s time-emitter removed from `main.go`.
- 2026-05-13: `main.go` wired with `application.SingleInstanceOptions` (uniqueID `com.taskboard.tbgui`); window opens at 1280×800, MacBackdropTranslucent, `MacTitleBarHiddenInset`.
- 2026-05-13: `frontend/src/routes/+page.svelte` replaced with three-column placeholder shell (real kanban lands in TB-23). `frontend/bindings/` regenerated (empty service set for now).
- 2026-05-13: `go build ./gui` → exit 0 (linker warns about macOS SDK version mismatch — cosmetic, doesn't affect functionality). Frontend `npm run build` → "Wrote site to dist".
- 2026-05-13: Runtime tests (`wails3 dev`, second-instance focus) deferred to the end-of-epic `/ui-test` pass.
- 2026-05-13: Done — TB-18..TB-22 unblocked.
- 2026-05-13: Done
