# TB-15: Add flag.NewFlagSet and reorderArgs to tb show

**Type:** tech-debt
**Priority:** P2
**Size:** S
**Module:** cli
**Tags:** milestone-m1,cli-ergonomics
**Branch:** —
**Parent:** TB-1

## Goal

tb show currently only handles a single positional argument. Add flag.NewFlagSet plus the existing reorderArgs helper so both tb show 1 --json and tb show --json 1 work — needed because the GUI exec wrapper appends --json after positional args.

## Context

`tb show` currently consumes a single positional argument with no flag parsing — so `tb show 1 --json` won't work cleanly. Other commands use `reorderArgs` + `flag.NewFlagSet` to accept flags in any position. Bring `show` in line so the GUI exec wrapper can append `--json` after the positional ID.

## Acceptance Criteria

- [ ] `cli/show.go` uses `flag.NewFlagSet` with a `--json` flag
- [ ] `reorderArgs` is called before flag parsing, like other commands
- [ ] Both `tb show 1 --json` and `tb show --json 1` work
- [ ] `tb show 1` (no flag) still prints the raw markdown to stdout
- [ ] Existing usage in `board/SKILL.md` / `CONVENTIONS.md` still works

## Related Tasks

- **TB-1** — Parent epic
- **TB-8** — Prerequisite
- **TB-12** — Sister task (provides `--json` behaviour)

## Log

- 2026-05-13: Created
- 2026-05-13: Started — moved to in-progress
- 2026-05-13: Done
