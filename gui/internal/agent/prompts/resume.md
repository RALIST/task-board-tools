The previous run has a captured agent session. Inspect the current worktree and board state, then continue from where that session left off. Do not restart the task unless continuation is impossible.
