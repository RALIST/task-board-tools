# tb project config
# Generated by `tb init`; rerun `tb init` to refresh with .bak backups.

# Board directory relative to this file.
board: board

# Task ID prefix.
prefix: TB

# Warn when starting a task above this many in-progress tasks.
# wip_limit: 2

# File extensions scanned by `tb scan`.
# scan_extensions: .go,.ts,.svelte,.js,.tsx,.jsx
